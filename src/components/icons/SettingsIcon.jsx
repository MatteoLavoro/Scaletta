import { Settings2 } from "lucide-react";

const SettingsIcon = ({ className = "w-6 h-6", ...props }) => (
  <Settings2 className={className} strokeWidth={2} {...props} />
);

export default SettingsIcon;
