import { MoreHorizontal } from "lucide-react";

const MoreHorizontalIcon = ({ className = "w-6 h-6", ...props }) => (
  <MoreHorizontal className={className} strokeWidth={2} {...props} />
);

export default MoreHorizontalIcon;
