import { MoreVertical } from "lucide-react";

const MoreVerticalIcon = ({ className = "w-6 h-6", ...props }) => (
  <MoreVertical className={className} strokeWidth={2} {...props} />
);

export default MoreVerticalIcon;
